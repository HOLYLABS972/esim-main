/**
 * Firebase config - STUBBED OUT
 * Firebase has been removed. All functionality migrated to Supabase.
 * This file exists only to prevent import errors from any remaining references.
 */

console.warn('⚠️ firebase/config.js is stubbed - Firebase has been removed. Use Supabase instead.');

export const auth = null;
export const db = null;
export const storage = null;
export const functions = null;
export const remoteConfig = null;
export default null;
